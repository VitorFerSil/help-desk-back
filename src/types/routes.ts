export enum ROUTES {
  ORGANIZATION = 'organization',
  ORGANIZATION_ALL = 'organization/*',
  USER = 'user',
  USER_ALL = 'user/*',
  DEMAND = 'demand',
  DEMAND_ALL = 'demand/*',
  ORGANIZATION_ROLES = 'organization-roles',
  ORGANIZATION_ROLES_ALL = 'organization-roles/*',
  AUTH = 'auth',
  AUTH_ALL = 'auth/*',
  CLIENT = 'client',
  PERMISSIONS = 'permissions'
}

export enum ROUTES_INITIAL {
  INITIAL_CONFIG = 'initial-config'
}
